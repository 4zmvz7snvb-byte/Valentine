
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Be My Valentine 💘</title>

  <!-- Handwritten font -->
  <link href="https://fonts.googleapis.com/css2?family=Patrick+Hand&display=swap" rel="stylesheet">

  <style>
    * {
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
      user-select: none;
    }

    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #ffd6e8, #ffe9c6);
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      overflow: hidden;
    }

    .container {
      max-width: 420px;
      padding: 20px;
      position: relative;
      z-index: 2;
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 10px;
    }

    p {
      font-size: 1rem;
      margin-bottom: 20px;
    }

    img {
      width: 100%;
      max-width: 280px;
      border-radius: 16px;
      margin-bottom: 25px;
      transition: transform 0.3s ease;
    }

    .buttons {
      position: relative;
      height: 140px;
    }

    button {
      padding: 12px 22px;
      font-size: 1rem;
      border: none;
      border-radius: 25px;
      cursor: pointer;
      position: absolute;
      white-space: nowrap;
    }

    #yesBtn {
      background-color: #ff5fa2;
      color: white;
      left: 50%;
      transform: translateX(-50%);
    }

    #yesBtn:hover {
      transform: translateX(-50%) scale(1.1);
    }

    #noBtn {
      background-color: #ddd;
      color: #333;
      left: 50%;
      top: 70px;
      transform: translateX(-50%);
    }

    /* Confetti */
    .confetti {
      position: fixed;
      top: -10px;
      width: 10px;
      height: 10px;
      opacity: 0.8;
      animation: fall 3s linear forwards;
      z-index: 1;
    }

    @keyframes fall {
      to {
        transform: translateY(110vh) rotate(720deg);
        opacity: 0;
      }
    }

    /* Handwritten note overlay */
    .note-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.45);
      display: none;
      justify-content: center;
      align-items: center;
      z-index: 5;
    }

    .note {
      background: #fffaf0;
      padding: 30px;
      max-width: 330px;
      border-radius: 12px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.25);
      font-family: 'Patrick Hand', cursive;
      font-size: 1.15rem;
      line-height: 1.6;
      transform: scale(0.8);
      animation: popIn 0.4s ease forwards;
      text-align: left;
    }

    @keyframes popIn {
      to {
        transform: scale(1);
      }
    }

    .note h2 {
      margin-top: 0;
      text-align: center;
      font-weight: normal;
    }
  </style>
</head>

<body>
  <div class="container">
    <h1 id="question">Will you be my Valentine? 💖</h1>
    <p id="subtext">Please think very carefully 😭👉👈</p>

    <!-- Beggy GIF -->
    <img
      id="gif"
      src="https://media.giphy.com/media/3oriO0OEd9QIDdllqo/giphy.gif"
      alt="Pleaseee"
    />

    <div class="buttons">
      <button id="yesBtn">Yes 💕</button>
      <button id="noBtn">No 😶</button>
    </div>
  </div>

  <!-- Handwritten Note -->
  <div class="note-overlay" id="noteOverlay">
    <div class="note">
      <h2>💌</h2>
      <p>
        I’m sorry for not getting you flowers yesterday unlike PRATHAM.
        But i’ve got you something you’ll like more than just a few flowers
        and something you can have with you for longer 😘.
      </p>
    </div>
  </div>

  <script>
    const noBtn = document.getElementById("noBtn");
    const yesBtn = document.getElementById("yesBtn");
    const gif = document.getElementById("gif");
    const question = document.getElementById("question");
    const subtext = document.getElementById("subtext");
    const noteOverlay = document.getElementById("noteOverlay");

    let noAttempts = 0;

    const noTexts = [
      "No 😶",
      "Are you sure? 😐",
      "Think again 🤨",
      "That’s not happening 😤",
      "Nice try 😌",
      "Okay wow 😭",
      "This button has trust issues"
    ];

    function moveNoButton() {
      noAttempts++;
      noBtn.textContent = noTexts[Math.min(noAttempts, noTexts.length - 1)];

      const padding = 20;
      const maxX = window.innerWidth - noBtn.offsetWidth - padding;
      const maxY = window.innerHeight - noBtn.offsetHeight - padding;

      noBtn.style.left = Math.random() * maxX + "px";
      noBtn.style.top = Math.random() * maxY + "px";
    }

    noBtn.addEventListener("mouseenter", moveNoButton);
    noBtn.addEventListener("touchstart", e => { e.preventDefault(); moveNoButton(); });
    noBtn.addEventListener("click", e => { e.preventDefault(); moveNoButton(); });

    yesBtn.addEventListener("click", () => {
      question.textContent = "YAYYY 💕 I knew you'd say yes!";
      subtext.textContent = "Best Valentine ever 🥰";
      gif.src = "https://media.giphy.com/media/l0MYt5jPR6QX5pnqM/giphy.gif";
      gif.style.transform = "scale(1.1)";
      noBtn.style.display = "none";
      launchConfetti();

      // Show handwritten note after a short pause
      setTimeout(() => {
        noteOverlay.style.display = "flex";
      }, 800);
    });

    function launchConfetti() {
      for (let i = 0; i < 80; i++) {
        const confetti = document.createElement("div");
        confetti.className = "confetti";
        confetti.style.left = Math.random() * 100 + "vw";
        confetti.style.backgroundColor =
          `hsl(${Math.random() * 360}, 100%, 70%)`;
        confetti.style.animationDuration = 2 + Math.random() * 2 + "s";
        document.body.appendChild(confetti);

        setTimeout(() => confetti.remove(), 4000);
      }
    }

    // Close note on tap/click
    noteOverlay.addEventListener("click", () => {
      noteOverlay.style.display = "none";
    });
  </script>
</body>
</html>